--- Template for stay open scripts that can use the day of the week as a trigger
--- change the day to whatever you want

set datte to current date

set dayy to weekday of datte as text
if dayy contains "Monday" then
	--- enter your command here
	
end if
